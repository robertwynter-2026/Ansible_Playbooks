---
plugin: amazon.aws.aws_ec2

regions:
  - us-east-1

filters:
  tag:Project: aws-k8s-platform
  tag:Environment: dev

keyed_groups:
  - key: tags.Role
    prefix: role

  - key: tags.Name
    prefix: name

hostnames:
  - tag:Name
